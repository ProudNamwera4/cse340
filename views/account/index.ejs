<h1>
    <%= title %>
</h1>

<%- messages() %>
<h4>You are logged in!</h4>
<% if (errors) { %>
    <ul class="notice">
   <% errors.array().forEach(error => { %>
     <li><%= error.msg %></li>
  <%  }) %>
   </ul>
  <% } %>